<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Connexion</title>
  <!-- Bootstrap CSS (CDN ou local) -->
  <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
  >
  <!-- Icônes Bootstrap (optionnel) -->
  <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
  >
  <style>
    html,
    body {
      height: 100%;
      margin: 0;
      padding: 0;
      font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: radial-gradient(circle at 15% 20%, rgba(116, 185, 255, 0.35), transparent 45%),
        radial-gradient(circle at 85% 0%, rgba(9, 132, 227, 0.3), transparent 55%),
        linear-gradient(135deg, #f6f8fb 0%, #eef1f6 100%);
      color: #1e272e;
    }

    .login-wrapper {
      position: relative;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100%;
      padding: 2.5rem 1.5rem;
    }

    .floating-shape {
      position: absolute;
      border-radius: 50%;
      filter: blur(0);
      opacity: 0.28;
      z-index: 0;
      background: linear-gradient(135deg, rgba(9, 132, 227, 0.55), rgba(116, 185, 255, 0.55));
    }

    .floating-shape.shape-1 {
      width: 320px;
      height: 320px;
      top: -120px;
      right: -100px;
    }

    .floating-shape.shape-2 {
      width: 220px;
      height: 220px;
      bottom: 40px;
      left: -80px;
      background: linear-gradient(135deg, rgba(30, 55, 90, 0.4), rgba(9, 132, 227, 0.45));
    }

    .login-card {
      position: relative;
      display: flex;
      width: min(900px, 95vw);
      border-radius: 22px;
      overflow: hidden;
      box-shadow: 0 30px 60px rgba(30, 55, 90, 0.18);
      background: rgba(255, 255, 255, 0.92);
      backdrop-filter: blur(12px);
      z-index: 1;
    }

    .brand-panel {
      flex: 1 1 48%;
      padding: 3rem;
      background: linear-gradient(160deg, #1e375a 0%, #0f82d0 60%, #4fa7ff 100%);
      color: #fff;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .brand-panel .logo-mark {
      display: inline-flex;
      align-items: center;
      gap: 0.75rem;
      font-size: 1.125rem;
      font-weight: 600;
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }

    .brand-panel .logo-circle {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 42px;
      height: 42px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.18);
      font-size: 1.35rem;
    }

    .brand-panel h2 {
      margin-top: 2.5rem;
      font-size: 2.1rem;
      font-weight: 600;
      line-height: 1.25;
    }

    .brand-panel p {
      margin-top: 1rem;
      font-size: 1rem;
      color: rgba(255, 255, 255, 0.8);
      line-height: 1.6;
    }

    .brand-panel .brand-footer {
      margin-top: auto;
      font-size: 0.85rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: rgba(255, 255, 255, 0.55);
    }

    .form-panel {
      flex: 1 1 52%;
      padding: 3rem;
      background: #fff;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .form-panel h1 {
      font-size: 1.9rem;
      font-weight: 600;
      margin-bottom: 0.75rem;
    }

    .form-panel p.subtitle {
      color: #6c7a89;
      margin-bottom: 2rem;
    }

    .form-label {
      font-weight: 600;
      color: #1e272e;
      font-size: 0.95rem;
    }

    .input-wrapper {
      position: relative;
      display: flex;
      align-items: center;
      margin-top: 0.35rem;
    }

    .input-wrapper .bi {
      position: absolute;
      left: 1rem;
      font-size: 1.1rem;
      color: #8e9ba8;
    }

    .form-control {
      border: 1px solid #dfe6ee;
      border-radius: 12px;
      padding: 0.75rem 1rem 0.75rem 2.85rem;
      box-shadow: none;
      transition: border-color 0.2s ease, box-shadow 0.2s ease;
    }

    .form-control:focus {
      border-color: #0f82d0;
      box-shadow: 0 0 0 4px rgba(15, 130, 208, 0.15);
    }

    .btn-primary {
      background: linear-gradient(135deg, #0f82d0, #3aa0f8);
      border: none;
      border-radius: 14px;
      font-size: 1rem;
      font-weight: 600;
      padding: 0.75rem;
      box-shadow: 0 14px 30px rgba(15, 130, 208, 0.25);
      transition: transform 0.2s ease, box-shadow 0.2s ease, filter 0.2s ease;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 20px 35px rgba(15, 130, 208, 0.3);
      filter: brightness(1.03);
    }

    .btn-primary:active {
      transform: translateY(0);
    }

    .form-footer {
      margin-top: 1.75rem;
      text-align: center;
      font-size: 0.9rem;
      color: #6c7a89;
    }

    .form-footer a {
      color: #0f82d0;
      text-decoration: none;
      font-weight: 600;
    }

    .form-footer a:hover {
      text-decoration: underline;
    }

    .alert-danger {
      border: none;
      border-radius: 12px;
      background: rgba(214, 48, 49, 0.12);
      color: #c0392b;
      font-size: 0.9rem;
      padding: 1rem 1.25rem;
    }

    @media (max-width: 900px) {
      .login-card {
        flex-direction: column;
      }

      .brand-panel,
      .form-panel {
        flex: unset;
        width: 100%;
        padding: 2.5rem 2rem;
      }

      .brand-panel {
        text-align: center;
      }

      .brand-panel h2 {
        font-size: 1.8rem;
      }

      .brand-panel .brand-footer {
        margin-top: 2rem;
      }
    }
  </style>
</head>
<body>

<div class="login-wrapper">
  <span class="floating-shape shape-1"></span>
  <span class="floating-shape shape-2"></span>

  <div class="login-card">
    <div class="brand-panel">
      <div class="logo-mark">
        <span class="logo-circle"><i class="bi bi-building"></i></span>
        Batirenov
      </div>
      <div>
        <h2>Bienvenue sur l'espace de gestion Batirenov</h2>
        <p>
          Accédez à vos outils de pilotage, suivez les opérations et
          assurez-vous que vos équipes disposent de tout le matériel nécessaire.
        </p>
      </div>
      <div class="brand-footer">Gestion des stocks &middot; Logistique &middot; Performance</div>
    </div>
    <div class="form-panel">
      <h1>Connexion sécurisée</h1>
      <p class="subtitle">Veuillez renseigner vos identifiants professionnels.</p>

      <!-- Affichage des erreurs -->
      <% if (errors && errors.length > 0) { %>
        <div class="alert alert-danger">
          <% errors.forEach(function(error) { %>
            <p class="mb-0"><%= error %></p>
          <% }) %>
        </div>
      <% } %>

      <!-- Formulaire de connexion -->
      <form action="/auth/login" method="POST">
        <div class="mb-4">
          <label for="email" class="form-label">Adresse e-mail</label>
          <div class="input-wrapper">
            <i class="bi bi-envelope"></i>
            <input
              type="email"
              name="email"
              id="email"
              class="form-control"
              placeholder="nom.prenom@batirenov.info"
              required
            >
          </div>
        </div>

        <div class="mb-3">
          <label for="password" class="form-label">Mot de passe</label>
          <div class="input-wrapper">
            <i class="bi bi-shield-lock"></i>
            <input
              type="password"
              name="password"
              id="password"
              class="form-control"
              placeholder="••••••"
              required
            >
          </div>
        </div>

        <div class="d-grid mt-4">
          <button type="submit" class="btn btn-primary">
            <i class="bi bi-box-arrow-in-right me-1"></i> Se connecter
          </button>
        </div>
      </form>

      <div class="form-footer">
        Besoin d'aide ? <a href="mailto:support@batirenov.info">Contactez le support</a>
      </div>
    </div>
  </div>
</div>

<!-- Bootstrap JS (CDN ou local) -->
<script
  src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js">
</script>
</body>
</html>
