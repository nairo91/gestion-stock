<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Connexion</title>
  <!-- Bootstrap CSS (CDN ou local) -->
  <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
  >
  <!-- Icônes Bootstrap (optionnel) -->
  <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
  >
  <style>
    html,
    body {
      height: 100%;
      margin: 0;
      padding: 0;
      font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: radial-gradient(circle at 20% 15%, rgba(74, 0, 224, 0.35), transparent 45%),
        radial-gradient(circle at 90% 10%, rgba(7, 61, 171, 0.4), transparent 55%),
        linear-gradient(135deg, #040511 0%, #0d0e2b 55%, #1a103d 100%);
      color: #f5f7ff;
    }

    .login-wrapper {
      position: relative;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100%;
      padding: 2.5rem 1.5rem;
    }

    .floating-shape {
      position: absolute;
      border-radius: 50%;
      filter: blur(12px);
      opacity: 0.3;
      z-index: 0;
      background: linear-gradient(135deg, rgba(61, 20, 139, 0.7), rgba(8, 104, 181, 0.55));
    }

    .floating-shape.shape-1 {
      width: 360px;
      height: 360px;
      top: -150px;
      right: -120px;
    }

    .floating-shape.shape-2 {
      width: 240px;
      height: 240px;
      bottom: 20px;
      left: -90px;
      background: linear-gradient(135deg, rgba(24, 16, 69, 0.6), rgba(7, 61, 171, 0.55));
    }

    .login-card {
      position: relative;
      display: flex;
      width: min(900px, 95vw);
      border-radius: 22px;
      overflow: hidden;
      box-shadow: 0 30px 70px rgba(3, 8, 33, 0.55);
      background: rgba(7, 10, 30, 0.85);
      backdrop-filter: blur(16px);
      z-index: 1;
    }

    .brand-panel {
      flex: 1 1 48%;
      padding: 3rem;
      background: linear-gradient(160deg, #040516 0%, #110b33 55%, #221255 100%);
      color: #fff;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      position: relative;
      overflow: hidden;
    }

    .brand-panel::after {
      content: '';
      position: absolute;
      inset: 0;
      background: radial-gradient(circle at top right, rgba(80, 54, 185, 0.45), transparent 55%),
        radial-gradient(circle at bottom left, rgba(25, 101, 185, 0.35), transparent 60%);
      z-index: 0;
    }

    .brand-panel .logo-mark {
      position: relative;
      display: inline-flex;
      align-items: center;
      gap: 0.75rem;
      font-size: 1.125rem;
      font-weight: 600;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      z-index: 1;
    }

    .brand-panel .logo-circle {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 46px;
      height: 46px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.12);
      font-size: 1.35rem;
      border: 1px solid rgba(255, 255, 255, 0.22);
      backdrop-filter: blur(2px);
    }

    .brand-panel .logo-image {
      position: relative;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 120px;
      height: 120px;
      margin-bottom: 1.5rem;
      border-radius: 18px;
      background: rgba(255, 255, 255, 0.08);
      border: 1px dashed rgba(255, 255, 255, 0.3);
      overflow: hidden;
      z-index: 1;
    }

    .brand-panel .logo-image img {
      width: 100%;
      height: 100%;
      object-fit: contain;
    }

    .brand-panel h2 {
      position: relative;
      margin-top: 2rem;
      font-size: 2.1rem;
      font-weight: 600;
      line-height: 1.25;
      z-index: 1;
    }

    .brand-panel p {
      position: relative;
      margin-top: 1rem;
      font-size: 1rem;
      color: rgba(255, 255, 255, 0.82);
      line-height: 1.6;
      z-index: 1;
    }

    .brand-panel .brand-footer {
      position: relative;
      margin-top: auto;
      font-size: 0.85rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: rgba(226, 228, 255, 0.58);
      z-index: 1;
    }

    .form-panel {
      flex: 1 1 52%;
      padding: 3rem;
      background: rgba(5, 8, 24, 0.82);
      display: flex;
      flex-direction: column;
      justify-content: center;
      color: #f5f7ff;
      position: relative;
      z-index: 1;
    }

    .form-panel h1 {
      font-size: 1.9rem;
      font-weight: 600;
      margin-bottom: 0.75rem;
    }

    .form-panel p.subtitle {
      color: rgba(217, 222, 255, 0.7);
      margin-bottom: 2rem;
    }

    .form-label {
      font-weight: 600;
      color: rgba(237, 239, 255, 0.88);
      font-size: 0.95rem;
    }

    .input-wrapper {
      position: relative;
      display: flex;
      align-items: center;
      margin-top: 0.35rem;
    }

    .input-wrapper .bi {
      position: absolute;
      left: 1rem;
      font-size: 1.1rem;
      color: rgba(167, 179, 208, 0.85);
    }

    .form-control {
      border: 1px solid rgba(116, 129, 171, 0.35);
      border-radius: 12px;
      padding: 0.75rem 1rem 0.75rem 2.85rem;
      box-shadow: none;
      background: rgba(10, 13, 36, 0.9);
      color: #f5f7ff;
      transition: border-color 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
    }

    .form-control:focus {
      border-color: rgba(113, 135, 255, 0.85);
      box-shadow: 0 0 0 4px rgba(113, 135, 255, 0.18);
      background: rgba(15, 22, 58, 0.95);
    }

    .form-control::placeholder {
      color: rgba(245, 247, 255, 0.65);
    }

    .form-control[type="password"],
    input[type="password"].form-control,
    input[type="password"] {
      color: #f5f7ff !important;
    }

    .form-control[type="password"]::placeholder,
    input[type="password"].form-control::placeholder {
      color: rgba(245, 247, 255, 0.65);
    }

    .form-control:-webkit-autofill,
    .form-control:-webkit-autofill:hover,
    .form-control:-webkit-autofill:focus {
      -webkit-text-fill-color: #f5f7ff;
      transition: background-color 9999s ease-in-out 0s;
      box-shadow: 0 0 0px 1000px rgba(10, 13, 36, 0.9) inset;
    }

    .btn-primary {
      background: linear-gradient(135deg, #3a4bff, #9f3aff);
      border: none;
      border-radius: 14px;
      font-size: 1rem;
      font-weight: 600;
      padding: 0.75rem;
      box-shadow: 0 18px 32px rgba(82, 58, 255, 0.35);
      transition: transform 0.2s ease, box-shadow 0.2s ease, filter 0.2s ease;
      color: #fff;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 22px 38px rgba(122, 78, 255, 0.45);
      filter: brightness(1.05);
    }

    .btn-primary:active {
      transform: translateY(0);
    }

    .form-footer {
      margin-top: 1.75rem;
      text-align: center;
      font-size: 0.9rem;
      color: rgba(217, 222, 255, 0.7);
    }

    .form-footer a {
      color: #8a9bff;
      text-decoration: none;
      font-weight: 600;
    }

    .form-footer a:hover {
      text-decoration: underline;
    }

    .alert-danger {
      border: none;
      border-radius: 12px;
      background: rgba(214, 48, 49, 0.18);
      color: #ffb3b3;
      font-size: 0.9rem;
      padding: 1rem 1.25rem;
    }

    @media (max-width: 900px) {
      .login-card {
        flex-direction: column;
      }

      .brand-panel,
      .form-panel {
        flex: unset;
        width: 100%;
        padding: 2.5rem 2rem;
      }

      .brand-panel {
        text-align: center;
      }

      .brand-panel .logo-image {
        margin: 0 auto 1.75rem;
      }

      .brand-panel h2 {
        font-size: 1.8rem;
      }

      .brand-panel .brand-footer {
        margin-top: 2rem;
      }
    }
  </style>
</head>
<body>

<div class="login-wrapper">
  <span class="floating-shape shape-1"></span>
  <span class="floating-shape shape-2"></span>

  <div class="login-card">
    <div class="brand-panel">
      <div class="logo-image">
        <!-- Remplacez "logo.png" par votre fichier (format recommandé : PNG ou SVG)
             à déposer dans "public/images/" -->
        <img src="/images/logo.png" alt="Logo Batirenov" onerror="this.style.display='none'">
      </div>
      <div class="logo-mark">
        <span class="logo-circle"><i class="bi bi-building"></i></span>
        Batirenov
      </div>
      <div>
        <h2>Bienvenue sur l'espace de gestion Batirenov</h2>
        <p>
          Accédez à vos outils de pilotage, suivez les opérations et
          assurez-vous que vos équipes disposent de tout le matériel nécessaire.
        </p>
      </div>
      <div class="brand-footer">Gestion des stocks &middot; Logistique &middot; Performance</div>
    </div>
    <div class="form-panel">
      <h1>Connexion sécurisée</h1>
      <p class="subtitle">Veuillez renseigner vos identifiants professionnels.</p>

      <!-- Affichage des erreurs -->
      <% if (errors && errors.length > 0) { %>
        <div class="alert alert-danger">
          <% errors.forEach(function(error) { %>
            <p class="mb-0"><%= error %></p>
          <% }) %>
        </div>
      <% } %>

      <!-- Formulaire de connexion -->
      <form action="/auth/login" method="POST">
        <div class="mb-4">
          <label for="email" class="form-label">Adresse e-mail</label>
          <div class="input-wrapper">
            <i class="bi bi-envelope"></i>
            <input
              type="email"
              name="email"
              id="email"
              class="form-control"
              placeholder="nom.prenom@batirenov.info"
              required
            >
          </div>
        </div>

        <div class="mb-3">
          <label for="password" class="form-label">Mot de passe</label>
          <div class="input-wrapper">
            <i class="bi bi-shield-lock"></i>
            <input
              type="password"
              name="password"
              id="password"
              class="form-control"
              placeholder="••••••"
              required
            >
          </div>
        </div>

        <div class="d-grid mt-4">
          <button type="submit" class="btn btn-primary">
            <i class="bi bi-box-arrow-in-right me-1"></i> Se connecter
          </button>
        </div>
      </form>

      <div class="form-footer">
        Besoin d'aide ? <a href="mailto:support@batirenov.info">Contactez le support</a>
      </div>
    </div>
  </div>
</div>

<!-- Bootstrap JS (CDN ou local) -->
<script
  src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js">
</script>
</body>
</html>
