<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Importer du Matériel via Excel</title>
  <!-- Utilisation des styles Bootstrap et de la feuille de style globale -->
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    :root {
      --violet-fonce: #1a0638;
      --violet: #4b1b7b;
      --violet-clair: #7e49b0;
      --blanc-translucide: rgba(255, 255, 255, 0.92);
    }

    body {
      background: radial-gradient(circle at top, rgba(126, 73, 176, 0.25), transparent 55%),
                  linear-gradient(135deg, var(--violet-fonce) 0%, #100225 45%, #1f0a3a 100%);
      color: #f8f9fc;
      font-family: "Segoe UI", Roboto, sans-serif;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2.5rem 1.5rem;
    }

    .container {
      width: min(840px, 100%);
    }

    .page-header {
      text-align: center;
      margin-bottom: 2rem;
    }

    .brand-logo {
      height: 72px;
      width: auto;
      margin-bottom: 1rem;
      filter: drop-shadow(0 6px 18px rgba(0, 0, 0, 0.35));
    }

    .page-title {
      font-size: 2rem;
      font-weight: 700;
      color: #ffffff;
      letter-spacing: 0.02em;
    }

    .page-subtitle {
      font-size: 1rem;
      color: rgba(255, 255, 255, 0.7);
      margin-top: 0.35rem;
    }

    .form-card {
      background: var(--blanc-translucide);
      border-radius: 1rem;
      box-shadow: 0 20px 50px rgba(10, 5, 30, 0.35);
      padding: 2.25rem 2.5rem;
      backdrop-filter: blur(6px);
    }

    .form-card label,
    .form-card .form-text {
      color: #2f1a4a;
    }

    .form-label {
      font-weight: 600;
      letter-spacing: 0.01em;
    }

    .form-select,
    .form-control {
      border: 1px solid rgba(75, 27, 123, 0.35);
      border-radius: 0.65rem;
      padding: 0.75rem 1rem;
      font-size: 1rem;
      transition: border-color 0.2s ease, box-shadow 0.2s ease;
    }

    .form-select:focus,
    .form-control:focus {
      border-color: var(--violet);
      box-shadow: 0 0 0 0.2rem rgba(75, 27, 123, 0.25);
    }

    input[type="file"]::file-selector-button {
      background-color: rgba(75, 27, 123, 0.12);
      border: 1px solid rgba(75, 27, 123, 0.35);
      border-radius: 0.5rem;
      padding: 0.45rem 0.85rem;
      margin-right: 1rem;
      font-weight: 600;
      color: var(--violet);
      transition: background-color 0.2s ease, color 0.2s ease;
    }

    input[type="file"]::file-selector-button:hover {
      background-color: var(--violet);
      color: #ffffff;
    }

    .btn-success {
      background: linear-gradient(135deg, var(--violet) 0%, #35115c 100%);
      border-color: transparent;
      font-weight: 700;
      padding: 0.65rem 1.65rem;
      border-radius: 0.65rem;
      letter-spacing: 0.01em;
      box-shadow: 0 15px 30px rgba(75, 27, 123, 0.35);
      transition: transform 0.15s ease, box-shadow 0.2s ease;
    }

    .btn-success:hover {
      transform: translateY(-2px);
      box-shadow: 0 18px 35px rgba(53, 17, 92, 0.45);
    }

    .btn-secondary {
      color: var(--violet);
      text-decoration: none;
      font-weight: 600;
      margin-left: 1rem;
      transition: color 0.2s ease;
    }

    .btn-secondary:hover {
      color: var(--violet-clair);
      text-decoration: underline;
    }

    @media (max-width: 576px) {
      body {
        padding: 1.5rem 1rem;
      }

      .form-card {
        padding: 1.75rem 1.5rem;
      }

      .btn-success {
        width: 100%;
      }

      .btn-secondary {
        display: inline-block;
        margin-left: 0;
        margin-top: 1rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="page-header">
      <img src="/images/logo.png" alt="Logo de l'entreprise" class="brand-logo" onerror="this.style.display='none'">
      <h2 class="page-title">Importer du Matériel via Excel</h2>
      <p class="page-subtitle">Ajoutez rapidement vos fournitures de chantier grâce à votre modèle Excel.</p>
    </div>
    <div class="form-card">
      <form action="/chantier/import-excel" method="POST" enctype="multipart/form-data">
        <div class="mb-3">
          <label for="chantierId" class="form-label">Chantier</label>
          <select name="chantierId" id="chantierId" class="form-select" required>
            <option value="">-- Sélectionner un chantier --</option>
            <% (chantiers || []).forEach(function(ch) { %>
              <option value="<%= ch.id %>" <%= String(ch.id) === String(selectedChantierId) ? 'selected' : '' %>><%= ch.nom %> - <%= ch.localisation %></option>
            <% }); %>
          </select>
        </div>
        <div class="mb-3">
          <label for="excel" class="form-label">Fichier Excel</label>
          <input type="file" id="excel" name="excel" class="form-control" accept=".xlsx,.xls" required>
          <div class="form-text">Les colonnes <strong>LOT</strong>, <strong>Désignation</strong>, <strong>Fournisseur</strong> et <strong>Qte</strong> seront utilisées. Les autres colonnes sont ignorées.</div>
        </div>
        <button type="submit" class="btn btn-success">Importer</button>
        <a href="/chantier" class="btn btn-secondary">Annuler</a>
      </form>
    </div>
  </div>
</body>
</html>
