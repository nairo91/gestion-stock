<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Fiche du matériel chantier</title>
  <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-4">
    <h2>Détails du matériel : <%= mc.materiel.nom %></h2>

    <div class="card mb-4">
      <div class="card-body">
        <p><strong>Référence :</strong> <%= mc.materiel.reference || '-' %></p>
        <p><strong>Description :</strong> <%= mc.materiel.description || '-' %></p>
        <p><strong>Catégorie :</strong> <%= mc.materiel.categorie || '-' %></p>
        <p><strong>Quantité sur le chantier :</strong> <%= mc.quantite %></p>
        <p><strong>Chantier :</strong> <%= mc.chantier.nom %></p>
        <p><strong>Emplacement :</strong> <%= mc.materiel.emplacement ? mc.materiel.emplacement.nom : '-' %></p>
        <p><strong>Rack :</strong> <%= mc.materiel.rack || '-' %></p>
        <p><strong>Compartiment :</strong> <%= mc.materiel.compartiment || '-' %></p>
        <p><strong>Niveau :</strong> <%= mc.materiel.niveau || '-' %></p>

        <% if (mc.materiel.photos && mc.materiel.photos.length > 0) { %>
          <p><strong>Photo :</strong></p>
          <img src="/<%= mc.materiel.photos[0].chemin %>" style="max-width: 300px; border: 1px solid #ccc;">
        <% } else { %>
          <p><strong>Photo :</strong> -</p>
        <% } %>
      </div>
    </div>

    <h4>Historique des actions sur ce matériel</h4>
    <% if (historique.length === 0) { %>
      <p>Aucune action enregistrée.</p>
    <% } else { %>
      <table class="table table-bordered table-sm mt-3">
        <thead class="table-light">
          <tr>
            <th>Date</th>
            <th>Action</th>
            <th>Par</th>
            <th>Ancienne Qte</th>
            <th>Nouvelle Qte</th>
          </tr>
        </thead>
        <tbody>
          <% historique.forEach(h => { %>
            <tr>
              <td><%= new Date(h.createdAt).toLocaleString('fr-FR') %></td>
              <td><%= h.action %></td>
              <td><%= h.user ? h.user.nom : 'Inconnu' %></td>
              <td><%= h.oldQuantite ?? '-' %></td>
              <td><%= h.newQuantite ?? '-' %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>

    <a href="/chantier" class="btn btn-secondary mt-3">Retour</a>
  </div>

  <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>
